import "./Header.scss";
import React from "react";
import PropTypes from "prop-types";

const Header = () => {
  return (
    <header className="header">
      <h1>Tetris</h1>
    </header>
  );
};

Header.propTypes = {}

export default Header;
